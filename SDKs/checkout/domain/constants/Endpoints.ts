export namespace Endpoints {
    export namespace Base {

        export namespace Auth {
            export const SANDBOX_URL = "https://access.sandbox.checkout.com";
            export const PRODUCTION_URL = "https://access.checkout.com";
        };

        export namespace Api {
            export const SANDBOX_URL = "https://api.sandbox.checkout.com";
            export const PRODUCTION_URL = "https://api.checkout.com";
        };
    };
}